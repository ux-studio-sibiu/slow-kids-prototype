import CalendarAside from "@/app/components/calendar/calendar-aside";

// Left panel for /calendar: hero image with the calendar floating over it.
// Selecting a marked day routes the right panel to /calendar/[date].
export default function CalendarAsidePage() {
  return <CalendarAside />;
}
